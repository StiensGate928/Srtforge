# Runtime libs (install Torch + NeMo via installer after Torch)
# Typer's "all" extra was removed in v0.20.0; install the base package instead.
typer>=0.12.3
rich>=13.7.1
tqdm>=4.66.4
pyyaml>=6.0.1
requests>=2.32.3
# GUI runtime
PySide6>=6.7.0
# NeMo ASR depends on NumPy <2.0 today. Keep NumPy below 2.x so NeMo and the
# audio-separator dependency resolver can agree.
numpy>=1.24.0,<2.0.0
pyarrow>=20.0.0,<21
matplotlib>=3.8.0
soundfile>=0.12.1
watchdog>=4.0.0
# audio-separator 0.31+ requires NumPy >=2.0, which conflicts with NeMo's
# current NumPy constraint. Pin to the latest compatible release on NumPy 1.x.
audio-separator>=0.30.2,<0.31.0
cuda-python>=12.3,<13
faster-whisper
ctranslate2==4.6.3
google-genai
nemo_toolkit[asr]>=2.5.1,<2.6
